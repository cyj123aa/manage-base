server:
  port: 9102
spring:
  application:
    name: manage-base
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  main:
    web-application-type: none
management:
  endpoints:
    web:
      exposure:
        include: refresh,health
